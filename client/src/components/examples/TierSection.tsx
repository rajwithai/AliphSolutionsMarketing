import TierSection from '../TierSection';
import { LanguageProvider } from '../LanguageProvider';

export default function TierSectionExample() {
  return (
    <LanguageProvider>
      <TierSection />
    </LanguageProvider>
  );
}